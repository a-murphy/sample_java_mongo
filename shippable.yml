language: java

env:
  - global: TEST=test1

#jdk:
#   - oraclejdk7
#   - openjdk6
#   - openjdk7
build:
  ci:
    - if [ "$BRANCH" == "test1" && "$IS_PULL_REQUEST" == "false" ]; then echo "versionName=$BRANCH.$BUILD_NUMBER" >> $JOB_STATE/image-resource.env; fi
notifications:
  email: false

