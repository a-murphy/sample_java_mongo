language: java

env:
  - global: TEST=test1

build:
  ci:
    - if [ "$BRANCH" == "test1" ]; then echo "versionName=$BRANCH.$BUILD_NUMBER" >> $JOB_STATE/image-resource.env; fi

notifications:
  email: false

