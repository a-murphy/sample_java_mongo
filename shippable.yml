language: java

jdk:
   - openjdk6

services:
   - mongodb
   
build:
  pre_ci_boot:
    image_name: drydock/u14javall
    image_tag: prod
    pull: true
  ci:
    - ant clean
    - ant test
    - ant coverage
 
notifications:
  email: false
