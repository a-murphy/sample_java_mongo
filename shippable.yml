language: java

jdk:
   - openjdk6

services:
   - mongodb

build:
  ci:
    - ant clean
    - ant coverage

integrations:
  notifications:
    - integrationName: "DV Image"
      type: webhook
      payload:
        - versionName=version
        - test1=1
        - test2=2
        - FOO=BAR
        - FIZZ=BUZZ
      on_success: always
      on_failure: always
      on_pull_request: never
      on_start: always

notifications:
  email: false

