language: java

env:
  global:
    - TEST=test1
jdk:
  - openjdk8
  - openjdk7
  - openjdk6

#matrix:
#  include:
#   - rvm: 2.0.0
#   - jdk: openjdk6

services:
   - mongodb
build:
  ci:
    - touch shippable/codecoverage/test.xml
    - ant clean
#    - ant coverage

integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: always
      on_failure: always
      on_pull_request: never
      on_start: always
      
